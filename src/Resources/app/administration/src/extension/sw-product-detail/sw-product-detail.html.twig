{% block sw_product_detail_actions %}
    {% parent %}

    <vivatura-translator-button
        v-if="product && product.id"
        entity-type="product"
        :entity-id="product.id"
        :disabled="isLoading"
        @translation-complete="onSaveFinish"
    ></viv-translator-button>
{% endblock %}

{% block sw_cms_detail_actions %}
    {% parent %}

    <vivatura-translator-button
        v-if="page && page.id"
        entity-type="cms-page"
        :entity-id="page.id"
        :disabled="isLoading"
        @translation-complete="onPageSave"
    ></vivatura-translator-button>
{% endblock %}
